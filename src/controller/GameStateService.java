package controller;



// TODO

//import com.google.gson.Gson;
//import model.gameState.GameState;

import java.io.FileReader;
import java.io.FileWriter;
import java.io.IOException;



/**
 * This file was supposed to contain the functionality for save the game variables into
 * Json format, it's currently unfinished and nonfunctional.
 * */
public class GameStateService {
//    private static final String SAVE_FILE = "game_state.json";
//
//    public static void saveGameState(Controller controller) {
//        try (FileWriter writer = new FileWriter(SAVE_FILE)) {
//            Gson gson = new Gson();
//            GameState gameState = controller.getSerializableGameState();
//            String json = gson.toJson(gameState);
//            writer.write(json);
//        } catch (IOException e) {
//            e.printStackTrace();
//        }
//    }
//
//    public static GameState loadGameState() {
//        try (FileReader reader = new FileReader(SAVE_FILE)) {
//            Gson gson = new Gson();
//            return gson.fromJson(reader, GameState.class);
//        } catch (IOException e) {
//            e.printStackTrace();
//            return null;
//        }
//    }
}

